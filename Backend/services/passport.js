import {Strategy as GoogleStrategy } from 'passport-google-oauth20';
import {Google} from '../models/User.js';
import dotenv from 'dotenv';
import { Passport } from 'passport';

dotenv.config();

export default (passport) => {
    passport.use(
        
    )
}